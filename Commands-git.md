Les commandes qui permet de :
    - cloner un repository 
        git clone <file>

    - push 
        git push --all
        git push origin master
        git push --force

    - créer une nouvelle branche
        git branch <file>

    - switcher de branche 
        git checkout <file>

        
        Je modifie.
